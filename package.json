{
  "name": "wrapjsx",
  "main": "lib/main.js",
  "type": "module",
  "description": "A way to use React without JSX",
  "version": "0.0.1",
  "scripts": {
    "dev": "coffee --watch --compile --output lib src"
  },
  "devDependencies": {
    "coffeescript": "^2.5.1"
  },
  "dependencies": {
    "react": ">=17"
  }
}
